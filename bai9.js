let a = 5;
let b= `5`;
console.log(a==b);         
// đây là phép so sánh giá trị giữa a và b 
console.log(a===b);
// đây là phép so sánh giá trị và kiểu dữ liệu của a so với b
console.log(a!=b);  
// đây là   phép so sánh xem giá trị a có khác giá trị b hay không
console.log(a!==b);
// đây là phép so sánh giá trị và kiểu dữ liệu giữa a và b
